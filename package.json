{
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node ./generateStakerConfigs.js",
    "release": "npm version patch && git push origin --tags"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
