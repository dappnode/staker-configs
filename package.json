{
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "build": "node ./generateStakerConfigs.js",
    "release": "npm version patch && git push --follow-tags"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
